{
  "dependencies": {
    "@hookland/dev-tools-commons": "^0.0.1",
    "fireworks-js": "^2.10.8",
    "lerna": "^8.1.9",
    "nx": "^20.3.0",
    "zod": "^3.24.2"
  },
  "workspaces": [
    "packages/inject",
    "packages/example/simple",
    "packages/inject-testing-library",
    "packages/dev-tools-chrome",
    "packages/dev-tools-client",
    "packages/dev-tools-commons",
    "packages/tiny-state"
  ],
  "scripts": {
    "publish": "lerna run clean && lerna run build && lerna run test && lerna run lint && lerna publish",
    "lint": "lerna run lint",
    "netlify": "cd packages/www && yarn install && yarn build",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "name": "hookland",
  "license": "MIT",
  "version": "0.0.0",
  "npmClient": "npm"
}
